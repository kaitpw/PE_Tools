<UserControl x:Class="AddinCmdPalette.Core.InfoPopover"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:AddinCmdPalette.Core"
             xmlns:actions="clr-namespace:AddinCmdPalette.Actions;assembly=AddinCmdPalette"
             MinWidth="300"
             MaxWidth="500">
    <Border Padding="0"
            Background="{DynamicResource BackgroundFillColorPrimaryBrush}"
            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect Color="Black"
                              Opacity="0.3"
                              BlurRadius="20"
                              ShadowDepth="8"
                              Direction="270" />
        </Border.Effect>
        <Grid>
            <!-- Tooltip Content -->
            <TextBlock x:Name="TooltipTextBlock"
                       TextWrapping="Wrap"
                       FontFamily="Segoe UI"
                       FontSize="12"
                       LineHeight="18"
                       Padding="16,12"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                       Visibility="Collapsed" />

            <!-- Actions Content -->
            <ItemsControl x:Name="ActionsItemsControl"
                          Visibility="Collapsed">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="Transparent"
                                Padding="12,8"
                                Cursor="Hand"
                                MouseLeftButtonUp="ActionItem_MouseLeftButtonUp">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource BackgroundFillColorSecondaryBrush}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="{Binding Name}"
                                           FontFamily="Segoe UI"
                                           FontSize="12"
                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                           VerticalAlignment="Center" />

                                <TextBlock Grid.Column="1"
                                           x:Name="ShortcutTextBlock"
                                           Loaded="ShortcutTextBlock_Loaded"
                                           FontFamily="Segoe UI"
                                           FontSize="11"
                                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                           VerticalAlignment="Center"
                                           Margin="12,0,0,0" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
