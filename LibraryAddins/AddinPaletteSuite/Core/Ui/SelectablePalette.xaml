<UserControl x:Class="AddinPaletteSuite.Core.Ui.SelectablePalette"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:AddinPaletteSuite.Core.Ui"
             VerticalAlignment="Top">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/PE_Tools;component/addinpalettesuite/core/ui/wpfuiresources.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <!-- Note: Brushes are set in code-behind for hot-reload support -->
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">
        <!-- Main container -->
        <Border x:Name="MainBorder"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Background="{ui:ThemeResource ApplicationBackgroundBrush}">
            <Grid HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Search Box -->
                <Border x:Name="SearchBoxBorder"
                        ClipToBounds="True"
                        Background="{ui:ThemeResource ApplicationBackgroundBrush}">
                    <ui:TextBox x:Name="SearchTextBox"
                                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                BorderBrush="Transparent"
                                Background="Transparent"
                                CaretBrush="{ui:ThemeResource TextFillColorSecondaryBrush}"
                                Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}">
                        <ui:TextBox.InputBindings>
                            <KeyBinding Key="Down" Command="{Binding MoveSelectionDownCommand}" />
                            <KeyBinding Key="Up" Command="{Binding MoveSelectionUpCommand}" />
                        </ui:TextBox.InputBindings>
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    Padding="{TemplateBinding Padding}">
                                                <ScrollViewer x:Name="PART_ContentHost"
                                                              Margin="0"
                                                              Focusable="False" />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </ui:TextBox>
                </Border>

                <!-- Item List -->
                <Border Grid.Row="1" Padding="4,0,4,0">
                    <local:SelectableListView x:Name="ItemListView"
                                              ItemsSource="{Binding FilteredItems}"
                                              SelectedItem="{Binding SelectedItem}"
                                              SelectedIndex="{Binding SelectedIndex}" />
                </Border>

                <!-- Status Bar -->
                <Border x:Name="StatusBarBorder"
                        Grid.Row="2"
                        Background="{ui:ThemeResource CardBackgroundFillColorSecondaryBrush}">
                    <Grid x:Name="StatusBarGrid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                    <ui:TextBlock x:Name="ItemCountText"
                               Grid.Column="0"
                               Text="{Binding FilteredItems.Count, StringFormat=\{0\} items}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Left"
                               Style="{DynamicResource CaptionTextBlockStyle}"
                               Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" />
                    
                    <ui:TextBlock x:Name="HelpText"
                               Grid.Column="1"
                               Text="↑↓ Navigate • ← Tooltip • → Actions • Click/Enter Execute • Esc Close"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"
                               Style="{DynamicResource CaptionTextBlockStyle}"
                               Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" />
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>