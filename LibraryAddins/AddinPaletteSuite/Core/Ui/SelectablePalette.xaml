<UserControl x:Class="AddinPaletteSuite.Core.Ui.SelectablePalette"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:AddinPaletteSuite.Core.Ui"
             VerticalAlignment="Top">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="WpfUiResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <SolidColorBrush x:Key="TextPrimaryBrush" Color="{x:Static local:ThemeManager.TextPrimaryColor}" />
            <SolidColorBrush x:Key="TextSecondaryBrush" Color="{x:Static local:ThemeManager.TextSecondaryColor}" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">
        <!-- Main container -->
        <Border x:Name="MainBorder"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
            <Grid HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Search Box -->
                <Border x:Name="SearchBoxBorder"
                        ClipToBounds="True">
                    <ui:TextBox x:Name="SearchTextBox"
                                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                BorderBrush="Transparent"
                                Background="Transparent"
                                Foreground="{StaticResource TextPrimaryBrush}"
                                CaretBrush="{StaticResource TextSecondaryBrush}">
                        <ui:TextBox.InputBindings>
                            <KeyBinding Key="Down" Command="{Binding MoveSelectionDownCommand}" />
                            <KeyBinding Key="Up" Command="{Binding MoveSelectionUpCommand}" />
                        </ui:TextBox.InputBindings>
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    Padding="{TemplateBinding Padding}">
                                                <ScrollViewer x:Name="PART_ContentHost"
                                                              Margin="0"
                                                              Focusable="False" />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </ui:TextBox>
                </Border>

                <!-- Item List -->
                <Border Grid.Row="1" Padding="4,0,4,0">
                    <local:SelectableListView x:Name="ItemListView"
                                              ItemsSource="{Binding FilteredItems}"
                                              SelectedItem="{Binding SelectedItem}"
                                              SelectedIndex="{Binding SelectedIndex}" />
                </Border>

                <!-- Status Bar -->
                <Border x:Name="StatusBarBorder"
                        Grid.Row="2">
                    <Grid x:Name="StatusBarGrid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="1"
                                    Orientation="Horizontal"
                                    VerticalAlignment="Center">
                            <TextBlock x:Name="ItemCountText"
                                       Text="{Binding FilteredItems.Count, StringFormat=\{0\} items}"
                                       VerticalAlignment="Center" />
                            <TextBlock x:Name="HelpText"
                                       Text="↑↓ Navigate • ← Tooltip • → Actions • Click/Enter Execute • Esc Close"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>